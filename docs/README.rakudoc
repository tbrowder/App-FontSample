=begin pod

=TITLE App::FontSample

=SUBTITLE Create PDF font samples from C<PDF::Content::FontObj> objects

=head1 DESCRIPTION

C<App::FontSample> creates PDF font specimens, font comparisons, compact font
collections, and character charts.

C<App::FontSample> does not provide a font collection. Library callers supply
one or more C<PDF::Content::FontObj> objects. The installed C<font-sample>
program can load an OTF or TTF file directly.

The current release supports Letter and A4 paper, portrait and landscape
orientation, configurable margins, JSON job files, registered language
pangrams, and four layouts:

=item C<specimen>
=item C<comparison>
=item C<collection>
=item C<characters>

=head1 INSTALLATION

=begin code :lang<text>
zef install App::FontSample
=end code

=head1 FIRST SAMPLE

The easiest first test uses the standard PDF core font Times-Roman, so no
external font file is required.

=begin code :lang<raku>
use PDF::API6;
use App::FontSample;

my $pdf = PDF::API6.new;
my $font = $pdf.core-font: :family<Times-Roman>;

create-font-sample(
    $font,
    :name<Times-Roman>,
    :title<Font Specimen>,
    :language<en>,
    :output<sample.pdf>,
);
=end code

The specimen identifies the font, labels the alphabet and punctuation sizes,
labels the pangram as C<English (en)>, and includes a size waterfall with
each point size shown at the left.

=head1 CURRENT LAYOUTS

=head2 specimen

The C<specimen> layout produces one specimen page per font. It shows the font
name, alphabet, numerals and punctuation, a labeled language pangram, and a
size waterfall.

=begin code :lang<raku>
create-font-sample(
    $font,
    :name<Times-Roman>,
    :layout<specimen>,
    :title<Font Specimen>,
    :language<en>,
    :output<specimen.pdf>,
);
=end code

See an example at L<click here | documents/specimen.pdf>.


An explicit pangram must also identify its language so the PDF can label it:

=begin code :lang<raku>
create-font-sample(
    $font,
    :name<Times-Roman>,
    :layout<specimen>,
    :language<en>,
    :pangram('Pack my box with five dozen liquor jugs.'),
    :output<specimen-custom.pdf>,
);
=end code

=head2 comparison

The C<comparison> layout displays the same text in several fonts at one
stated point size. The font name is shown above each sample. The layout
continues onto additional pages when needed.

=begin code :lang<raku>
create-font-collection-sample(
    @entries,
    :layout<comparison>,
    :title<Font Comparison>,
    :comparison-size(18),
    :text(
        'The quick brown fox jumps over the lazy dog. 0123456789'
    ),
    :output<comparison.pdf>,
);
=end code

=head2 collection

The C<collection> layout is a compact one-page inventory of fonts. Each font
name is followed by a short typographic identification string. The page title
states the sample point size.

=begin code :lang<raku>
create-font-collection-sample(
    @entries,
    :layout<collection>,
    :title<Font Collection>,
    :sample-size(14),
    :text('Hamburgefonts 0123456789 Aa Bb Cc'),
    :output<collection.pdf>,
);
=end code

If the requested collection cannot fit within the page margins, the layout
reports an error rather than overflowing the page.

=head2 characters

The C<characters> layout displays selected characters in a grid. The page
heading identifies the font and glyph point size, and each cell labels the
glyph with its Unicode code point.

=begin code :lang<raku>
create-font-sample(
    $font,
    :name<Times-Roman>,
    :layout<characters>,
    :title<Character Sample>,
    :characters(
        'ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789'
    ),
    :columns(8),
    :glyph-size(30),
    :output<characters.pdf>,
);
=end code

Library callers may supply C<characters> as a string or as a positional
collection containing one-character strings, integer Unicode code points, or
strings such as C<U+0041>.

=head1 FONT COLLECTIONS

A comparison or collection uses C<App::FontSample::FontEntry> objects.

=begin code :lang<raku>
use App::FontSample;
use App::FontSample::FontEntry;

my @entries;

@entries.push: App::FontSample::FontEntry.new(
    :name<Times-Roman>,
    :font($times),
);

@entries.push: App::FontSample::FontEntry.new(
    :name<Helvetica>,
    :font($helvetica),
);

create-font-collection-sample(
    @entries,
    :layout<comparison>,
    :comparison-size(18),
    :output<comparison.pdf>,
);
=end code

=head1 PAPER AND PAGE OPTIONS

Both C<Letter> and C<A4> are supported. Pages may be portrait or landscape,
and the margin may be set in PDF points.

=begin code :lang<raku>
create-font-sample(
    $font,
    :name<Times-Roman>,
    :paper<A4>,
    :landscape,
    :margin(36),
    :language<en>,
    :output<a4-landscape.pdf>,
);
=end code

There are 72 PDF points in one inch.

=head1 LANGUAGE PANGRAMS

The current built-in pangram registry includes English under the two-letter
code C<en>.

=begin code :lang<raku>
create-font-sample(
    $font,
    :name<Times-Roman>,
    :language<en>,
    :output<english.pdf>,
);
=end code

The resulting specimen labels the text C<English (en) pangram>.

List the currently registered language codes with:

=begin code :lang<text>
font-sample --languages
=end code

Applications may register additional pangrams and language names at runtime.

=head1 OTF AND TTF FILES

The installed C<font-sample> program loads an OTF or TTF file directly:

=begin code :lang<text>
font-sample \
    --font=/path/to/font.otf \
    --output=sample.pdf \
    --layout=specimen \
    --language=en
=end code

Current direct-mode layout names are:

=begin code :lang<text>
specimen
comparison
collection
characters
=end code

=head1 JSON INPUT

C<font-sample> can read one or more jobs from a JSON definition file:

=begin code :lang<text>
font-sample --config=font-samples.json
=end code

For example:

=begin code :lang<json>
{
    "output": "output/sample.pdf",
    "layout": "specimen",
    "language": "en",
    "fonts": [
        {
            "file": "fonts/MyFont.otf",
            "name": "My Font"
        }
    ]
}
=end code

=head1 OPTIONAL NOTOFONTS-OT USE

C<NotoFonts-OT> is not a dependency of C<App::FontSample>. It is one
optional source of C<PDF::Content::FontObj> objects.

=begin code :lang<raku>
use NotoFonts-OT;
use App::FontSample;
use App::FontSample::FontEntry;

my @entries;

for <
    NotoSerif-Regular
    NotoSerif-Bold
    NotoSans-Regular
    NotoSans-Bold
> -> $code {
    my $font = get-loaded-font($code);

    @entries.push: App::FontSample::FontEntry.new(
        :name($code),
        :$font,
    );
}

create-font-collection-sample(
    @entries,
    :layout<comparison>,
    :comparison-size(18),
    :output<noto-comparison.pdf>,
);
=end code

=head1 EXAMPLES

C<examples/current-capabilities.raku> uses PDF core fonts and creates four
PDFs, one for each current layout:

=item C<example-specimen.pdf>
=item C<example-comparison.pdf>
=item C<example-collection.pdf>
=item C<example-characters.pdf>

Run the examples like this:

=begin code
raku -Ilib examples/current-capabilities.raku
=end code

=head1 TESTING

Run the normal test suite with C<mi6 test> (for modules under
C<App::Mi6> management) or C<zef .>.

=begin code :lang<text>
mi6 test
=end code

The normal test suite does not require C<NotoFonts-OT>.

=head1 FUTURE WORK

This README describes current capability only. Planned additions and changes
are maintained in C<docs/TODO.rakudoc>.

=head1 SOURCE

The project source is maintained in the C<tbrowder/App-FontSample> repository
on GitHub.

=head1 AUTHOR

Tom Browder <tbrowder@acm.org>

=head1 COPYRIGHT AND LICENSE

© 2026 Tom Browder

This library is free software; you may redistribute it or modify it under the Artistic License 2.0.

=end pod
